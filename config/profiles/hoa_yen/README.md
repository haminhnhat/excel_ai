# Profile: hoa_yen

Profile này được tạo bằng web onboarding wizard.

Trước khi dùng cho file thật hoặc báo cáo chính thức, hãy validate lại profile với workbook tương ứng.

Người dùng không kỹ thuật nên mở app bằng `Start_App.bat`, chọn profile `hoa_yen`, rồi chạy scenario trên tab `Run scenario`.
